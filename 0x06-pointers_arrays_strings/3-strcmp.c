#include "main.h"
/**
 * _strcmp - compares two strings
 *
 * @s1: string 1
 * @s2: string 2
 * Return: 0 if strings are equal, -ve if 1st is greater +ve if vice versa
 */
int _strcmp(char *s1, char *s2)
{
	int value;

	value = strcmp(s1, s2);
	return (value);
}
