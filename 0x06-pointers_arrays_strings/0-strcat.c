#include "main.h"
/**
 * _strcat - concatenates 2 strings
 *
 * @dest: first string
 * @src: second string
 * Return: poniter to str dest
 */
char *_strcat(char *dest, char *src)
{
	strcat(dest, src);
	return (dest);
}
